import { NightIconPipe } from './night-icon.pipe';

describe('NightIconPipe', () => {
  it('create an instance', () => {
    const pipe = new NightIconPipe();
    expect(pipe).toBeTruthy();
  });
});
